---
tags:
  - python/复盘
---

# 易混概念与练习复盘

返回 [[00 Python学习导航]] · 对照 [[01 Python知识图谱]]。

这页只记录“最容易混淆、最值得反复看”的东西。原始错误过程继续保留在各天 `今日练习.md` 中。

## 1. 机翻词语统一

| 机翻/别扭说法 | 建议统一说法 |
| --- | --- |
| 词典 | 字典（dictionary） |
| 密钥 | 键（key） |
| 词典理解 | 字典推导式（dictionary comprehension） |
| 列表解析 | 列表推导式（list comprehension） |
| 集合理解 | 集合推导式（set comprehension） |
| 三进制算符 | 三元表达式 / 条件表达式 |
| 关键词论元 | 关键字参数 |
| 归位排序 | 原地排序 |
| 迭代物 | 可迭代对象 |
| 旗帜 | 标志变量（flag） |

## 2. 函数相关

### print 与 return

```python
print(x)
```

只是显示给人看。

```python
return x
```

把结果交给调用者，并立即结束当前函数。

### append 不要赋值回原变量

错误：

```python
a = a.append(x)
```

`append()` 修改原列表，但返回 `None`。

正确：

```python
a.append(x)
```

### 方法调用用圆括号

```python
a.append(x)
```

不是：

```python
a.append[x]
```

## 3. 变量、引用与复制

```python
b = a
```

`a` 和 `b` 指向同一个对象。

```python
b = a.copy()
```

浅拷贝：外层列表独立，但内部可变对象仍可能共享。

```python
import copy
b = copy.deepcopy(a)
```

深拷贝：递归复制内部对象。

### == 与 is

- `==`：比较值是否相等。
- `is`：比较是不是同一个对象。

## 4. 循环与 return

一个常见错误是把 `return True` 放在循环内部太早的位置。

回文判断正确思路：

```text
发现任何一对不相等 → return False
整个循环结束都没发现问题 → return True
```

另一个常见点：

```python
return -1
```

如果表示“遍历完都没找到”，就应该放在循环结束后，而不是第一轮没找到就返回。

## 5. sort 与 sorted

| 写法 | 是否修改原数据 | 返回值 |
| --- | --- | --- |
| `nums.sort()` | 是 | `None` |
| `sorted(nums)` | 否 | 新列表 |

自定义排序：

```python
sorted(students, key=lambda student: student[1], reverse=True)
```

注意参数叫 `reverse`，没有 `d`。

## 6. lambda

```python
lambda x: x ** 2
```

相当于：

```python
def square(x):
    return x ** 2
```

当前阶段不需要强行到处用 lambda。重点是看懂，并能在 `sorted(key=...)`、`map()`、`filter()` 中写简单判断或取值逻辑。

## 7. map / filter / reduce

| 工具 | 作用 | 常见结构 |
| --- | --- | --- |
| `map` | 转换每一项 | `map(fn, iterable)` |
| `filter` | 保留满足条件的项 | `filter(fn, iterable)` |
| `reduce` | 多项累积成一个值 | `reduce(fn, iterable)` |

Python 3 中 `map()` 和 `filter()` 返回惰性迭代结果，因此想得到普通列表时常写：

```python
list(filter(...))
list(map(...))
```

同一个迭代器被完整消费后，再次 `list()` 通常会得到空列表。

## 8. 列表推导式

模板：

```python
[result for item in iterable if condition]
```

例如：

```python
[num ** 2 for num in nums if num % 2 == 0]
```

当前建议：先会普通 `for + if + append`，再改成推导式。

## 9. 字典重点

### 读取

```python
person["name"]
person.get("name")
```

区别：不存在的键用 `[]` 会 `KeyError`，`get()` 默认返回 `None`。

### 遍历

```python
for key, value in person.items():
    ...
```

这里同时用到了字典的 `items()` 和之前学过的序列解包。

## 10. 集合重点

原笔记里“集合是不可变元素列表”容易误解。

正确理解：
- `set` 本身通常是可变容器。
- 元素不能重复。
- 元素必须可哈希。
- 空集合是 `set()`，不是 `{}`。

### 四个最重要的集合运算

```python
a | b   # 并集
a & b   # 交集
a - b   # 差集
a ^ b   # 对称差
```

### 三个关系判断

```python
a.issubset(b)
a.issuperset(b)
a.isdisjoint(b)
```

## 11. for...else / while...else

最重要的记法：

> `else` 在“循环没有被 break 打断”时执行。

```python
for item in items:
    if found(item):
        break
else:
    print("没找到")
```

空列表也属于正常结束，所以会执行 `else`。

## 12. 从练习里暴露出的当前薄弱点

### 已经明显进步

- `for + if + append + return` 已经比较稳定。
- 能自己修正函数名/参数名混淆。
- 能独立写 `find_target()`、列表去重等基础逻辑。
- 已经手写出标准冒泡排序主体。
- Day03 能写列表推导式、filter、sorted + lambda。
- 遇到“遍历时修改列表”的疑问会主动做测试验证，而不是只听结论。

### 还需要继续练

1. 从空白处写 `lambda`、`key=` 时仍不够熟。
2. 迭代器 / 惰性计算的直觉还需要更多例子。
3. 综合题容易先想语法，而不是先拆步骤。
4. Day04 的字典与集合刚学，需要通过手写题把方法真正焊住。

## 13. 当前复习入口

- [[week01/day03/00 Day03整理版|Day03 整理版]]
- [[week01/day04/00 Day04整理版|Day04 整理版]]
- [[week01/day03/今日练习|Day03 原始练习记录]]

> [!note]
> 保留第一次错误代码很有价值。优化笔记的目标不是把过去写得像“从来没错过”，而是让你以后能快速看到：当时错在哪、现在应该怎么理解。
