---
tags:
  - python/复盘
---

# 易混概念与练习复盘

返回 [[00 Python学习导航]] · 对照 [[01 Python知识图谱]]。

这页只记录最容易混淆、最值得反复看的东西。原始练习题继续放在各天 `今日练习.md` 中。

## 1. 机翻词语统一

| 机翻/别扭说法 | 建议统一说法 |
| --- | --- |
| 词典 | 字典（dictionary） |
| 密钥 | 键（key） |
| 词典理解 | 字典推导式（dictionary comprehension） |
| 列表解析 | 列表推导式（list comprehension） |
| 集合理解 | 集合推导式（set comprehension） |
| 三进制算符 | 三元表达式 / 条件表达式 |
| 关键词论元 | 关键字参数 |
| 立场论元 | 位置参数 |
| 变数参数 | 可变数量参数 |
| 例外 | 异常（exception） |
| 归位排序 | 原地排序 |
| 迭代物 | 可迭代对象 |
| 旗帜 | 标志变量（flag） |
| 蟒蛇试试看 | Python `try` 语句 |

## 2. 函数相关

### print 与 return

```python
print(x)
```

只是把内容显示出来。

```python
return x
```

把结果交给调用者，并立即结束当前函数。

### return 的位置

一个很常见的错误是把 `return` 放进循环太早：

```python
for num in nums:
    if num == target:
        return num
    return -1
```

如果 `-1` 表示“整个列表都没找到”，应该等循环真正结束后再返回。

### 写完函数检查出口

如果前面一直在构造：

```python
new_scores = {}
```

最后却：

```python
return scores
```

那么前面的筛选虽然写对了，返回的还是旧数据。

写完函数最后问自己：

> 我真正算出来的结果变量是谁？`return` 的是不是它？

### append 不要赋值回原变量

错误：

```python
a = a.append(x)
```

`append()` 修改原列表，但返回 `None`。

正确：

```python
a.append(x)
```

### 方法调用用圆括号

```python
a.append(x)
```

不是：

```python
a.append[x]
```

## 3. 变量名、字符串与参数

```python
Alice
```

Python 会把它当成变量名。

```python
"Alice"
```

才是字符串。

例如：

```python
def get_score(scores, name):
    ...

get_score(scores, "Alice")
```

调用后，函数内部相当于：

```python
name = "Alice"
```

所以：

```python
scores[name]
```

等价于：

```python
scores["Alice"]
```

不要凭空写一个没有定义过的变量名。

## 4. list / dict / set 最容易混的地方

### 三种容器在解决不同问题

```text
list：保存一串有顺序的数据
dict：保存 key → value 的对应关系
set ：保存不重复元素，并处理集合关系
```

### 三种添加方式

```python
nums.append(10)          # list
skills.add("Python")     # set
scores["Tom"] = 78      # dict
```

### 三种空容器

```python
[]       # list
{}       # dict
set()    # set
```

特别注意：

```python
new_nums = ()
```

这是元组 `tuple`，不是列表，不能 `append()`。

## 5. 字典的 key 和 value

```python
scores = {
    "Tom": 78,
    "Alice": 95
}
```

```text
"Tom"   是 key
78      是 value
```

直接遍历字典默认拿 key：

```python
for x in scores:
    print(x)
```

同时拿 key 和 value：

```python
for name, score in scores.items():
    ...
```

这是字典 `items()` 加上序列解包。

## 6. 字典计数模式

```python
counts = {}

for word in words:
    if word not in counts:
        counts[word] = 1
    else:
        counts[word] += 1
```

思路：

```text
第一次出现 → 创建 key，值设为 1
再次出现   → 原来的值加 1
```

## 7. 不要一边遍历容器一边随意修改结构

例如遍历字典时直接删除其中的 key：

```python
for name, score in scores.items():
    if score < 60:
        del scores[name]
```

会改变正在遍历的字典大小，容易报错。

当前阶段更推荐新建结果容器。

## 8. 变量、引用与复制

```python
b = a
```

`a` 和 `b` 指向同一个对象。

```python
b = a.copy()
```

浅拷贝：外层容器独立，但内部可变对象仍可能共享。

```python
import copy
b = copy.deepcopy(a)
```

深拷贝：递归复制内部对象。

### == 与 is

- `==`：比较值是否相等。
- `is`：比较是不是同一个对象。

## 9. sort 与 sorted

| 写法 | 是否修改原数据 | 返回值 |
| --- | --- | --- |
| `nums.sort()` | 是 | `None` |
| `sorted(nums)` | 否 | 新列表 |

自定义排序：

```python
sorted(students, key=lambda student: student[1], reverse=True)
```

## 10. lambda

```python
lambda x: x ** 2
```

相当于：

```python
def square(x):
    return x ** 2
```

当前阶段不需要强行到处使用 lambda。

## 11. map / filter / reduce

| 工具 | 作用 | 常见结构 |
| --- | --- | --- |
| `map` | 转换每一项 | `map(fn, iterable)` |
| `filter` | 保留满足条件的项 | `filter(fn, iterable)` |
| `reduce` | 多项累积成一个值 | `reduce(fn, iterable)` |

Python 3 中 `map()` 和 `filter()` 返回惰性迭代结果，想得到普通列表时常写：

```python
list(filter(...))
list(map(...))
```

## 12. 推导式

列表：

```python
[result for item in iterable if condition]
```

字典：

```python
{key: value for item in iterable if condition}
```

当前建议仍然是：先能独立写普通 `for`，再压缩成推导式。

## 13. 集合的真正用途

```text
去重
快速判断元素是否存在
处理两组数据之间的关系
```

```python
a | b   # 并集
a & b   # 交集
a - b   # 差集
a ^ b   # 对称差
```

关系判断：

```python
a.issubset(b)
a.issuperset(b)
a.isdisjoint(b)
```

## 14. for...else / while...else

最重要的记法：

> `else` 在循环没有被 `break` 打断时执行。

```python
for item in items:
    if found(item):
        break
else:
    print("没找到")
```

如果在循环里直接 `return`，函数已经结束，后面的 `else` 自然也不会再执行。

## 15. Day05：星号最容易混的地方

### 定义函数时

```python
def func(*args):
```

把多个位置参数收集成：

```python
tuple
```

```python
def func(**kwargs):
```

把多个关键字参数收集成：

```python
dict
```

### 调用函数时

```python
func(*data)
```

把序列拆成多个位置参数。

```python
func(**config)
```

把字典拆成多个关键字参数。

最重要的记法：

```text
定义时 * / ** → 收集
调用时 * / ** → 解包
```

关联：[[week01/day05/Python args|*args]]、[[week01/day05/Python kwargs|**kwargs]]、[[week01/day05/Python 解包元组|解包]]。

## 16. Day05：异常不是“隐藏错误”

```python
try:
    ...
except ValueError:
    ...
```

含义是：

> 我知道这里可能出现 `ValueError`，所以提前定义处理办法。

不推荐一上来就：

```python
except:
    ...
```

因为这会把不同错误混在一起。

`finally` 的核心：

```text
无论有没有异常，最后都执行
```

关联：[[week01/day05/Python try…except|try...except]]、[[week01/day05/Python try…except…finally|finally]]。

## 17. Day05：模块、包、类型提示

```text
.py 文件      → 模块 module
模块的文件夹  → 包 package
```

`import` 找不到模块时，想到：

```text
模块名 / 项目结构 / 虚拟环境 / 依赖安装 / sys.path
```

以下划线开头的函数：

```python
def _helper():
    ...
```

只是“内部实现”的约定，并不是真正的访问权限。

类型提示：

```python
def get_user(user_id: int) -> str | None:
    ...
```

主要帮助人、IDE、检查工具和框架理解代码，Python 运行时通常不会自动强制类型。

Day05 主线：[[week01/day05/Day05 函数参数、异常与模块|Day05 函数参数、异常与模块]]。

## 18. 当前练习暴露出的薄弱点

### 已经开始稳定

- `for + if` 的基本结构已经能自己写。
- 集合交集、差集、超集关系上手比较快。
- 能自己完成字典计数逻辑。
- 能在提示后正确写出 `for...else` 查找。
- 会主动运行代码确认结果，而不是只凭感觉判断。

### 当前优先补的地方

1. **容器选择**：什么时候用 list、dict、set 还没有形成条件反射。
2. **字典的数据视角**：key、value、`items()` 之间仍容易混。
3. **变量与数据的关系**：变量名、字符串、函数参数偶尔会混成一团。
4. **数据流向**：新结果保存在哪个变量、最后 `return` 谁，需要更主动检查。
5. **循环控制流**：`return`、`break`、循环结束、`else` 谁先发生，还需要继续手写。
6. **参数收集与解包**：看到 `*` / `**` 时要先判断它是在函数定义还是函数调用。
7. **异常处理场景感**：不仅会背 `try...except`，还要知道什么失败值得捕获。
8. **模块化直觉**：开始从“一份代码”过渡到“多个文件怎么协作”。

## 19. 当前复习入口

- [[week01/day03/Day03 列表与迭代|Day03 列表与迭代]]
- [[week01/day04/Day04 字典与集合|Day04 字典与集合]]
- [[week01/day05/Day05 函数参数、异常与模块|Day05 函数参数、异常与模块]]
- [[week01/day03/今日练习|Day03 原始练习记录]]
- [[week01/day04/今日练习|Day04 原始练习记录]]

> [!note]
> 现阶段不需要追求“看完教程觉得全懂”。真正的目标是看到一个小问题时，逐渐能判断：数据应该放在哪种容器里、循环每一轮拿到了什么、函数参数如何进入、错误发生后如何处理、代码变多后应该放在哪个模块里。
