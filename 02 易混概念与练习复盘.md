---
tags:
  - python/复盘
---

# 易混概念与练习复盘

返回 [[00 Python学习导航]] · 对照 [[01 Python知识图谱]]。本页是补充速查，原学习过程保留在 [[今日练习]] 中。

## 机翻词语对照

| 原来容易误解的说法 | 统一用语 | 对应笔记 |
| --- | --- | --- |
| 三进制算符 | 三元表达式／条件表达式 | [[Python三进制算符]] |
| 名单、列表列表 | 列表、嵌套列表 | [[Python 列表]] |
| 弦、弦长、F弦 | 字符串、字符串长度、f-string | [[python字符串]] |
| 关键词论元 | 关键字参数 | [[Python 关键词参数]] |
| 断点、断开 | break 退出循环 | [[Python break]] |
| 归位排序 | 原地排序 | [[Python 排序列表]] |
| 可重复变量、迭代物 | 按语境区分可迭代对象与迭代器 | [[Python 迭代]] |

## 最容易混淆的行为

| 对比 | 记住什么 | 回看 |
| --- | --- | --- |
| `print(x)` / `return x` | 前者显示；后者把结果交给调用者，并结束本次函数调用 | [[Python 函数]] |
| `a.append(x)` / `a[x]` | 前者调用方法添加元素；后者取某个位置的元素 | [[Python 列表]] |
| `a.append(x)` / `a = a.append(x)` | append 返回 None；第二种写法让 a 变成 None | [[Python 列表]] |
| `a.sort()` / `sorted(a)` | 前者修改 a、返回 None；后者返回新列表 | [[Python 排序列表]]、[[Python sorted]] |
| `break` / `continue` / `pass` | 退出最内层循环／跳过本轮剩余部分／什么都不做 | [[Python break]]、[[Python continue]]、[[Python pass]] |
| `x in a` / `a.index(x)` | 判断存在／取首次出现位置；index 找不到抛出 ValueError | [[查找列表中元素的索引]] |
| `map` / `filter` / `reduce` | 转换每项／保留满足条件的项／累积成一个值 | [[Python map（） 函数转换列表元素]]、[[Python中筛选列表元素]]、[[Python的reduce（） 函数将列表简化为单一值]] |

上述容器操作可对照 [Python 数据结构教程](https://docs.python.org/3/tutorial/datastructures.html)；内置函数行为见 [Python 内置函数文档](https://docs.python.org/3/library/functions.html)。

## 用五道原练习串起知识

| 练习 | 知识链 | 复盘检查 |
| --- | --- | --- |
| [[今日练习#01｜自己实现列表求和|01 求和]] | [[Python 函数]] → [[For 循环]] → [[python赋值操作符]] | len 应作用于 nums；累积值先设为 0；空列表应得到 0 |
| [[今日练习#02｜筛选偶数（原题：输出奇偶）|02 偶数筛选]] | [[python算术]] → [[python if语句]] → [[Python 列表]] | append 用圆括号；最后 return 列表，再由调用处 print |
| [[今日练习#03｜查找目标元素|03 查找]] | [[For 循环]] → [[python比较]] → [[查找列表中元素的索引]] | 找到时返回；return -1 必须放在循环结束后 |
| [[今日练习#04｜列表去重|04 去重]] | [[Python 列表]] → [[查找列表中元素的索引]] → [[Python break]] | 维护结果列表；not in 决定是否追加；保留首次出现顺序 |
| [[今日练习#05｜第二大的不同数字|05 第二大]] | 去重 → [[Python 排序列表]] → [[Python 列表|负索引]] | 先去重再排序；还需要考虑不同数字不足两个时怎么办 |

> [!note] 保留错误尝试的意义
> 原练习中的第一次尝试、报错和个人评价均保留。代码块排版整齐，并不代表其中每一次尝试都是正确答案。

## 复习时再补两类边界

- **第二大的不同数字**：现有最终写法适用于至少有两个不同数字的输入。`[]`、`[5]`、`[5, 5]` 会在 `a[-2]` 处抛出 `IndexError`。先决定这种输入应返回什么或是否报错，再补实现。
- **迭代器耗尽**：对同一个 map/filter 结果调用两次 `list()`，第二次得到空列表。需要反复使用结果时，先保存第一次转换出来的列表。见 [[Python 迭代]]。

## 和 AI 学习的连接

现在的“列表 → 遍历 → 转换／筛选 → 汇总”已经是一条小型数据处理链。以后学习 NumPy、Pandas 时，可以继续比较“每项数据是什么、操作是否改变原数据、结果是什么类型”。先把已有代码写明白，再换到新的数据工具。
